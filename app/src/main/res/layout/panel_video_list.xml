<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <LinearLayout
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/primaryColor"
        >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="?attr/colorOnPrimary"
            android:gravity="center"
            android:layout_gravity="center"
            android:layout_marginStart="5dp"
            />
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_weight="1"
            android:gravity="end"
            >
            <ImageButton
                android:id="@+id/up_button"
                android:layout_width="@dimen/media_button_width"
                android:layout_height="@dimen/media_button_height"
                app:srcCompat="@drawable/ic_up"
                app:tint="?attr/colorOnPrimary"
                android:background="?attr/selectableItemBackground"
                />
            <ImageButton
                android:id="@+id/down_button"
                android:layout_width="@dimen/media_button_width"
                android:layout_height="@dimen/media_button_height"
                app:srcCompat="@drawable/ic_down"
                app:tint="?attr/colorOnPrimary"
                android:background="?attr/selectableItemBackground"
                />
            <ImageButton
                android:id="@+id/refresh_button"
                android:layout_width="@dimen/media_button_width"
                android:layout_height="@dimen/media_button_height"
                app:srcCompat="@drawable/ic_refresh"
                app:tint="?attr/colorOnPrimary"
                android:background="?attr/selectableItemBackground"
                />
            <ImageButton
                android:id="@+id/setting_button"
                android:layout_width="@dimen/media_button_width"
                android:layout_height="@dimen/media_button_height"
                app:srcCompat="@drawable/ic_setting"
                app:tint="?attr/colorOnPrimary"
                android:background="?attr/selectableItemBackground"
                />
        </LinearLayout>
    </LinearLayout>

    <io.github.toyota32k.boodroid.view.VideoListView
        android:id="@+id/video_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:choiceMode="singleChoice"
        android:background="@drawable/border_list_view"
        android:paddingBottom="@dimen/video_list_bottom_border_width"
        android:paddingStart="@dimen/video_list_side_border_width"
        android:paddingEnd="@dimen/video_list_side_border_width"
        />


</merge>